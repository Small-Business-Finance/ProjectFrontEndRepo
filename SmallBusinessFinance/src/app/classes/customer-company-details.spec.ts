import { CustomerCompanyDetails } from './customer-company-details';

describe('CustomerCompanyDetails', () => {
  it('should create an instance', () => {
    expect(new CustomerCompanyDetails()).toBeTruthy();
  });
});
