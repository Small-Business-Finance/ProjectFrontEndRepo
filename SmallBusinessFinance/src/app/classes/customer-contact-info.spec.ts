import { CustomerContactInfo } from './customer-contact-info';

describe('CustomerContactInfo', () => {
  it('should create an instance', () => {
    expect(new CustomerContactInfo()).toBeTruthy();
  });
});
